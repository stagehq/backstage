mutation UpdateReaction(
  $content: String!
  $ideaId: String
  $commentId: String
  $threadCommentId: String
  $type: ParentType
) {
  updateReaction(
    content: $content
    ideaId: $ideaId
    commentId: $commentId
    threadCommentId: $threadCommentId
    type: $type
  ) {
    id
  }
}
