query GetCurrentUser {
  currentUser {
    id
    alias
    email
    name
    firstName
    lastName
    fullName
    image
    coverImageUrl
    uploadCredit
    sites {
      subdomain
      modifiedAt
      tagline
      bio
      image
    }
    mainSite {
      subdomain
      modifiedAt
      tagline
      bio
    }
  }
}
