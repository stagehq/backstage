query GetCurrentUser {
  currentUser {
    id
    alias
    email
    name
    firstName
    lastName
    fullName
    image
    coverImageUrl
    sites {
      subdomain
      modifiedAt
      tagline
      bio
    }
    mainSite {
      subdomain
      modifiedAt
      tagline
      bio
    }
  }
}
