query GetCurrentUser {
  currentUser {
    id
    alias
    email
    name
    firstName
    lastName
    fullName
    image
    coverImageUrl
    sites {
      subdomain
      modifiedAt
    }
    mainSite {
      subdomain
      modifiedAt
    }
  }
}
