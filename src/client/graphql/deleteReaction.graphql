mutation DeleteReaction(
  $content: String!
  $ideaId: String
  $commentId: String
  $threadCommentId: String
  $type: ParentType
) {
  deleteReaction(
    content: $content
    ideaId: $ideaId
    commentId: $commentId
    threadCommentId: $threadCommentId
    type: $type
  ) {
    id
  }
}
