query GetSite($subdomain: String!) {
  getSite(subdomain: $subdomain) {
    id
    modifiedAt
    createdAt
    subdomain

    extensions {
      id
      modifiedAt
      createdAt
      sortOrder

      storeExtension {
        id
        modifiedAt
        createdAt
        name
        markdown
      }

      underlayingApis {
        id
        modifiedAt
        createdAt
        refreshToken
        accessToken

        apiConnector {
          id
          modifiedAt
          createdAt
          name
          markdown
        }

        apiResponses {
          id
          modifiedAt
          createdAt
          response

          apiConnectorRoute {
            id
            modifiedAt
            createdAt
            name
            url
          }
        }
      }
    }
  }
}
