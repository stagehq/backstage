mutation UpsertSite(
  $subdomain: String!
  $tagline: String!
  $bio: String!
  $layouts: String
) {
  upsertSite(
    subdomain: $subdomain
    tagline: $tagline
    bio: $bio
    layouts: $layouts
  ) {
    id
    subdomain
  }
}
