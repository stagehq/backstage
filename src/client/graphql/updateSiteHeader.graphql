mutation UpdateSiteHeader(
  $siteId: String!
  $bio: String
  $tagline: String
  $image: String
) {
  updateSiteHeader(siteId: $siteId, bio: $bio, tagline: $tagline, image: $image) {
    id
    subdomain
    bio
    tagline
    image
  }
}
