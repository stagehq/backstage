mutation UpdateSiteHeader(
  $subdomain: String!
  $bio: String!
  $tagLine: String!
) {
  updateSiteHeader(subdomain: $subdomain, bio: $bio, tagline: $tagLine) {
    id
    subdomain
    bio
    tagline
  }
}
